/**
 * LES BOUCLES
 * —
 * Exercice 3
 */

var resultat = 0;

for (let cpt = 0; cpt <= 200; cpt = cpt + 2) {
  resultat += cpt;
}

console.log(
  "La somme des 100 premiers nombres pairs est égale à : " + resultat
);
